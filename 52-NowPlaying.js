function findTheCurrentSong(arr){
    let songName = arr[0];
    let singer = arr[1];
    let songLength = arr[2];

    //Now Playing: Nelly – Number One [4:09]
    console.log(`Now Playing: ${singer} - ${songName} [${songLength}]`);
}